const recipient = 'James'
const sender = 'Sarfraz'

const email =  `Hey ${recipient} 
How is it Going? 
Cheers ${sender}`
console.log(email)

const bothParty = `the sender is ${sender} and the receiver is ${recipient}`
console.log(bothParty)
